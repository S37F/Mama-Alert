# Used by: npm run env:init  (and predev if client/.env is missing)

VITE_SUPABASE_URL=https://xxxxxxxxxxxx.supabase.co
VITE_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.replace-with-anon-key
VITE_API_URL=http://localhost:3000
# From POST /api/register/patient response field `sos_token` (for /demo real SOS button)
VITE_DEMO_SOS_TOKEN=
# Optional: hospital portal JWT from admin "Copy portal token"
VITE_HOSPITAL_PORTAL_TOKEN=
# Optional: UUID `status_token` for /status/demo-status-token-abc123 (defaults to seed Priya token)
VITE_DEMO_STATUS_TOKEN=
